#include <iostream>
#include "LinkedQueue.h"
#include "StaticQueue.h"

int main()
{
	/*
	LinkedQueue<int> lqueue;
	LinkedQueue<int> otherQueue;

	for(int i = 1; i < 10; ++i)
	{
		lqueue.push(i);
	}
	
	std::cout << "After pushing front element is: " << lqueue.frontEl() << std::endl;

	for (int i = 1; i < 10; ++i)
	{
		lqueue.pop();
		std::cout << "After popping front element is: " << lqueue.frontEl() << std::endl;
	}

	std::cout << "Empty queue? " << lqueue.empty() << std::endl;

	lqueue.push(1);
	lqueue.push(2);
	lqueue.push(3);
	lqueue.push(4);

	otherQueue = lqueue;

	std::cout << "Empty queue? " << lqueue.empty() << std::endl;

	for (int i = 0; i < 4; ++i)
	{

		std::cout << "Front element in copy queue is: " << otherQueue.frontEl() << std::endl;
		otherQueue.pop();
	}
	*/

	/*
	StaticQueue<int> stQueue;
	
	for (int i = 1; i < 10; ++i)
	{
		stQueue.push(i);
	}

	std::cout << "After pushing front element is: " << stQueue.frontEl() << std::endl;

	for (int i = 0; i < 8; ++i)
	{
		stQueue.pop();
		std::cout << "After popping front element is: " << stQueue.frontEl() << std::endl;
	}

	std::cout << "Empty queue? " << stQueue.empty() << std::endl;
	*/


	return 0;
}